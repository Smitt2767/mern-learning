# Environment
NODE_ENV=development

# Server
SERVER_PORT=5001
SERVER_URL=http://localhost:5001

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_NAME=

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# Web App
FRONTEND_URL=http://localhost:3000

# JWT — must match auth-server secrets exactly
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Token expiry in seconds (must match auth-server values)
JWT_ACCESS_TOKEN_EXPIRY_SECONDS=10080
JWT_REFRESH_TOKEN_EXPIRY_SECONDS=43200

EXECUTE_BOOT_SCRIPTS=false