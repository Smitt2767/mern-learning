Create a new API route for the backend.

Route: $ARGUMENTS

Steps:
1. Create a new route file in `backend/src/routes/`
2. Add proper TypeScript types
3. Add Zod validation for request body/params
4. Register the route in the server
5. Add error handling
