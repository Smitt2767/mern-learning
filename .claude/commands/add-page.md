Create a new page/route for the frontend.

Page: $ARGUMENTS

Steps:
1. Create a new route file in `frontend/src/routes/` following TanStack Router file-based routing conventions
2. Use TypeScript and functional components
3. Style with Tailwind CSS
4. Add proper loader if data fetching is needed
